{
  "name": "tree-sitter-lux",
  "version": "2.1.0",
  "description": "Tree-sitter parser for the Lux programming language",
  "keywords": [
    "lux",
    "tree-sitter",
    "parser"
  ],
  "homepage": "https://github.com/fachammer/tree-sitter-lux",
  "bugs": "https://github.com/fachammer/tree-sitter-lux/issues",
  "license": "MIT",
  "author": "Fabian Achammer <fabian.achammer@gmail.com> (https://github.com/fachammer)",
  "repository": "github:fachammer/tree-sitter-lux",
  "dependencies": {
    "nan": "^2.14.0"
  },
  "devDependencies": {
    "@commitlint/cli": "^8.3.5",
    "@commitlint/config-conventional": "^8.3.4",
    "@commitlint/prompt-cli": "^8.3.5",
    "@semantic-release/changelog": "^3.0.6",
    "@semantic-release/git": "^7.0.18",
    "commitizen": "^4.0.3",
    "cp-cli": "^2.0.0",
    "cross-var": "^1.1.0",
    "glob": "^7.1.6",
    "husky": "^3.1.0",
    "npm-watch": "^0.6.0",
    "rimraf": "^3.0.2",
    "semantic-release": "^17.2.3",
    "tree-sitter-cli": "^0.15.14"
  },
  "watch": {
    "test": {
      "patterns": [
        "grammar.js",
        "corpus/**/*"
      ],
      "extensions": "txt"
    }
  },
  "husky": {
    "hooks": {
      "commit-msg": "commitlint -E HUSKY_GIT_PARAMS"
    }
  },
  "config": {
    "cz-customizable": {
      "config": "commitlint.confg.js"
    }
  },
  "scripts": {
    "build": "tree-sitter generate",
    "prepare": "npm run build",
    "unit-test-only": "tree-sitter test",
    "unit-test": "npm run build && npm run unit-test-only",
    "example-test-only": "node examples/run-tests.js",
    "test": "npm run build && npm run unit-test-only && npm run example-test-only",
    "test:watch": "npm-watch test",
    "commit": "commit",
    "package-test-only": "rimraf .test && cp-cli test/ .test/ && cd .test && cross-var npm install tree-sitter ../tree-sitter-lux-$npm_package_version.tgz && node test.js",
    "package-test": "npm pack && npm run package-test-only",
    "preversion": "npm test && tree-sitter build-wasm",
    "postpublish": "rimraf .test && cp-cli test/ .test/ && cd .test && npm install tree-sitter tree-sitter-lux && node test.js",
    "semantic-release": "semantic-release"
  }
}
