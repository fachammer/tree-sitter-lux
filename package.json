{
  "name": "tree-sitter-lux",
  "version": "0.0.1",
  "description": "Tree-sitter parser for the Lux programming language",
  "author": "Fabian Achammer",
  "license": "MIT",
  "dependencies": {
    "nan": "^2.14.0"
  },
  "devDependencies": {
    "tree-sitter-cli": "^0.15.8"
  },
  "scripts": {
    "generate": "tree-sitter generate",
    "test-only": "tree-sitter test",
    "test": "npm run generate && npm run test-only",
    "prepare": "npm run generate && npm run test-only"
  }
}
